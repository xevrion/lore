name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Format
        run: pnpm exec oxfmt --check

      - name: Lint
        run: pnpm exec oxlint

      - name: Typecheck
        run: pnpm run check

      - name: Test
        run: pnpm run test

      - name: Build client
        run: pnpm run build

      - name: Migrations are listed
        run: |
          for f in migrations/*.sql; do
            grep -q "$(basename "$f")" migrations/README.md || { echo "$f is missing from migrations/README.md"; exit 1; }
          done

      - uses: actions/upload-artifact@v7
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          name: client-dist
          path: packages/client/dist
          if-no-files-found: error
          retention-days: 3

  deploy:
    name: Deploy
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Every step is skipped until the Cloudflare secrets exist, so a fresh fork
    # stays green before it is configured.
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v6
        if: env.CLOUDFLARE_API_TOKEN != ''

      - uses: pnpm/action-setup@v4
        if: env.CLOUDFLARE_API_TOKEN != ''

      - uses: actions/setup-node@v5
        if: env.CLOUDFLARE_API_TOKEN != ''
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
        if: env.CLOUDFLARE_API_TOKEN != ''

      - uses: actions/download-artifact@v8
        if: env.CLOUDFLARE_API_TOKEN != ''
        with:
          name: client-dist
          path: packages/client/dist

      - name: Apply migrations
        if: env.CLOUDFLARE_API_TOKEN != ''
        run: pnpm exec wrangler d1 migrations apply lore --remote

      - name: Deploy
        if: env.CLOUDFLARE_API_TOKEN != ''
        run: pnpm exec wrangler deploy
